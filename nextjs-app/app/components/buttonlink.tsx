import React from 'react';
import { Button } from 'antd';

export default function MyButton() {
    return (
        <div>
            <Button type="link">Sanity Create</Button>
        </div>
    );
}
